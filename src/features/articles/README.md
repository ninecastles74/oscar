# Articles feature

Reserved namespace for single-article views, raw article ingestion UI, and
article-level utilities. Currently a placeholder — the canonical `NewsArticle`
type lives in `src/types/news-platform.ts` and the mock article fixtures are
generated by `src/lib/mock-data/stories.ts`.

When the real fetch pipeline lands, put article list / detail components and
hooks here.
